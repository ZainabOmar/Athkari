// import library to help create a component:
import { AppRegistry } from 'react-native';

//create the component:
import App from './App';

//render the component to the screen:
AppRegistry.registerComponent('Athkari', () => App);
